---
const { href = "#", class: className = "", color = "#ffffff" } = Astro.props;
---

<!-- Placeholder STMPD wordmark (replace with exported SVG once selected in Figma) -->
<a
  href={href}
  class={"inline-flex items-center text-[16px] font-light leading-[19.04px] tracking-[0.36px] " + className}
  style={`color:${color};`}
>
  STMPD
</a>

